# Server
PORT=3500
BASE_URL=https://lillerbros.pcc2k.com

# Sessions (used by CSRF + rate limit). Generate fresh:
#   node -e "console.log(require('crypto').randomBytes(32).toString('hex'))"
SESSION_SECRET=changeme

# SMTP for the contact form. If absent, submissions still save to logs/.
SMTP_HOST=
SMTP_PORT=587
SMTP_SECURE=false
SMTP_USER=
SMTP_PASS=
SMTP_FROM="Liller Brothers <noreply@lillerbros.com>"

# Where contact-form messages get sent.
CONTACT_EMAIL=lillerbrothers@gmail.com
